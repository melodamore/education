import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ethiolearn.app',
  appName: 'EthioLearn',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;